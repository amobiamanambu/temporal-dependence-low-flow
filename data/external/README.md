# External inputs

Do not place raw data in Git. For a full reproduction, download USGS,
GAGES-II, and GridMET inputs into
`workflow/full_reproduction/` as described in `docs/data_sources.md`.

This directory is reserved for temporary local links or small user-supplied
inputs. Its contents are ignored by Git except for this README.
