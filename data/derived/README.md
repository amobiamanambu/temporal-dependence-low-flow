# Derived data

Use this directory for local derived results that are not part of the compact
example. Large continental outputs should be deposited in a versioned,
DOI-issuing data repository and referenced from `docs/data_availability.md`.
