# Example outputs

`python scripts/run_sample_workflow.py` regenerates the CSV tables under
`outputs/tables/` and the PNG/SVG figures under `outputs/figures/`.

The checked-in images provide a visual preview. They are demonstration outputs,
not substitutes for the full continental publication figures.
